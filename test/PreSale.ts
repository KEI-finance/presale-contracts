import { expect } from "chai";
import { ethers } from "hardhat";

import "@nomiclabs/hardhat-ethers";

describe("PreSale", () => {
  describe("deposit", () => {
    it("should increase msg.sender balance by msg.value", () => {});
  });
  describe("withdraw", () => {
    it("should transfer contract eth balance to withdrawal address", () => {});
  });
  describe("refund", () => {
    it("should refund msg.sender balance", () => {});
  });
});
